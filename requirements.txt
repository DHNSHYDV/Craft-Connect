# Production Server
gunicorn

# Core dependencies
Flask
python-dotenv
requests
gdown
flask-sqlalchemy
flask-login
groq
google-generativeai
huggingface_hub

# Note: torch, transformers, accelerate are EXCLUDED to fit Vercel 250MB limit.
# Local AI features (GLM-OCR, Whisper) will be disabled.
