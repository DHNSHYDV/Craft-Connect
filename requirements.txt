# Production Server
gunicorn

# Core dependencies
Flask
dotenv
requests
gdown
flask-sqlalchemy
flask-login
google-generativeai
huggingface_hub
replicate

# Optional: Heavy ML libraries (Commented out for free tier deployment to save space/build time)
# torch
# transformers
# accelerate